{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mesh",
  "description": "Phase 15 — mesh P2P relay: allows the webview to invoke mesh relay commands",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
